# ktmm (Keep That Mouse Moving!) - Go Version

This works the same as the [Python version](https://github.com/ao/ktmm), but written in Go so that it's cross-platform.

## How to run

### Build the app
`go build`

### Run the app
`go run .`

